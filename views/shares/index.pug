extends ../layout

block content
  h2 Your Shared Documents
  ul.shared-list
    for share in shares
      li
        if share.title
          a(href="/shares/" + share._id) #{share.title}
        else
          a(href="/shares/" + share._id) #{share._id}

